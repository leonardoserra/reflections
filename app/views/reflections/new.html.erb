<h1>New reflection</h1>

<%= form_with model: @document do |form| %>
  <div>
    <%= form.text_field :name, placeholder: "Name", autofocus: true %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

<%= link_to "Cancel", reflections_path %>
